import Link from "next/link"; import {projects} from "../../data/projects"; export default function Projects(){return <><header className="listing-head"><div className="eyebrow">Project archive</div><h1>Selected projects,<br/>without the noise.</h1><p>A curated collection of production work across Laravel, PHP, React, Vue, Node.js and WordPress. Each project opens into a focused case study.</p></header><main className="grid">{projects.map(p=><article className="card" key={p.slug}><div><div className="eyebrow">{p.category}</div><h2>{p.title}</h2><p>{p.summary}</p></div><div><div className="resultline"><b>{p.result}</b></div><div className="mini">{p.technology}</div><Link className="arrow" href={`/projects/${p.slug}`}>Open case study →</Link></div></article>)}</main><footer>Shaurya Dogra · Senior Full-Stack Laravel Developer</footer></>}